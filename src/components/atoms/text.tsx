import { css } from '@emotion/react';
import { fontSans, text3Xl, textBase } from '../../styles/typography';

export const title = css(text3Xl, fontSans);

export const text = css(textBase, fontSans);
